export interface IContactInputs {
    id: string;
    name: string;
    label: string;
    autoComplete?: string;
    extraClass?: string;
    rows?: number;
    defaultValue?: string;
    type?: string;
}