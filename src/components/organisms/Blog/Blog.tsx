import { BlogPosts, Footer, Header, Title } from "components"

export const Blog = () => {
    return (
        <div>
            <Header />
            <Title />
            <BlogPosts />
            <Footer />
        </div>
    )
}